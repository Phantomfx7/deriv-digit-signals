# Copy this file to .env and fill in your own Deriv app_id.
# Register one (free) at https://developers.deriv.com/docs/app-registration
# Not required to run the app — it falls back to the shared public id 1089 —
# but that shared id can be throttled/restricted, so your own is recommended.
VITE_DERIV_APP_ID=
